# Configura??o do Servidor para o cont?iner
#server.port=8085

# Configura??es do Actuator
#management.endpoints.web.exposure.include=*
#management.metrics.tags.application=microservice-radar-cart

# ==========================================================
# ##            CONFIGURA??O DO BANCO POSTGRESQL            ##
# ==========================================================
# Aponta para o servi?o 'postgres-db' que definiremos no Docker Compose
#spring.datasource.url=jdbc:postgresql://postgres-db:5432/cart_db
#spring.datasource.username=${POSTGRES_USER}
#spring.datasource.password=${POSTGRES_PASSWORD}
#spring.datasource.driverClassName=org.postgresql.Driver
#spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
# Em produ??o real, o ideal ? usar ferramentas de migra??o como Flyway ou Liquibase
#spring.jpa.hibernate.ddl-auto=update

# ==========================================================
# ##            CONFIGURA??ES DO RABBITMQ                 ##
# ==========================================================
# Aponta para o servi?o 'rabbitmq' no Docker Compose
#spring.rabbitmq.host=rabbitmq
#spring.rabbitmq.port=5672
#spring.rabbitmq.username=${RABBITMQ_USER}
#spring.rabbitmq.password=${RABBITMQ_PASSWORD}

# RabbitMQ Producer Configuration
#rabbitmq.exchange.name=radares_exchange
#rabbitmq.routing.key=radares.cart
